
import DynamicMapController from './dynamic_mapController'

export default class DynamicMapView extends DynamicMapController {
	constructor (util, map, mapGlobals) {
		super(util, map, mapGlobals);
	}

	init () {
		super.init();
	}
}